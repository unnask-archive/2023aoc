const std = @import("std");
const Allocator = std.mem.Allocator;

const in = @embedFile("example");

//Looks like the intended solution here is dynamic programming,
//This was a learning process, so might as well learn more about
//the desired solution rather than brute forcing it.

pub fn main() !void {}
